server:
  port: 8080

database:
  path: ./mini-api.db

jwt:
  secret: mock-jwt-secret-key-change-in-production

log:
  level: info

upload:
  # Directory for storing uploaded files (relative to working directory)
  dir: ./uploads
  # Maximum file upload size in megabytes
  max_size_mb: 2
